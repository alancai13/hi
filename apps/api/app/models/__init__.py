# app/models/ — Future ORM models (SQLAlchemy)
#
# This package is intentionally empty until a database is added (Phase 4).
# When persistence is introduced:
#   - Define SQLAlchemy model classes here (e.g. JobModel, ArtifactModel)
#   - Keep them separate from domain models in app/domain/models.py
#   - Add Alembic for migrations
